create table users(user_id bigint auto_increment, username varchar(255), password varchar(255))

--- mysql
--- create table users(user_id int primary key auto_increment, username varchar(255), password varchar(255))